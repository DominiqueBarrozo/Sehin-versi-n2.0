@model IEnumerable<Sehin_versión2._0.Models.Puestos>

@{
    ViewBag.Title = "Index";
}
<br><br><br>
<h2>Listado de puestos</h2>

<p>
    @Html.ActionLink("Crear puesto", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Puesto
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TareasPuestoId)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Borrar", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
